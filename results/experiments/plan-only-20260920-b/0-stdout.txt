Created `cold_io.py` with the two required functions:

- **`read_text(path)`** — accepts `str` or `pathlib.Path`, reads the entire file as UTF-8 text, and raises `FileNotFoundError` if the file doesn't exist.
- **`write_text(path, text, *, append=False)`** — accepts `str`/`Path` path and `str` text, writes UTF-8. Overwrites by default, appends when `append=True`, creates missing files, and does **not** create parent directories.

The trusted validator ran the checks and the module was created successfully (not reused or recovered). No other files were created.
