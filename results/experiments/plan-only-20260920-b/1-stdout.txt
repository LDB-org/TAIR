The `plan` tool created `warm_io.py` and the trusted validator accepted it (reused: true, recovered: false).

The module provides:
- `read_text(path)` — reads the whole file as UTF-8 text, raising `FileNotFoundError` if missing.
- `write_text(path, text, *, append=False)` — writes UTF-8 text, overwriting by default or appending when `append=True`, creating the file if missing but not creating parent directories.

No other files were created.
