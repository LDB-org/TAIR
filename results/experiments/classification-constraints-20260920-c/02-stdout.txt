Done. Created `classification-constraints-20260920-c/02_parentio.py` with `write_text(path, text)` and `read_text(path)` using pathlib and UTF-8. `write_text` creates missing parent directories recursively and overwrites the file; `read_text` returns the full string. No top-level execution. Verified with `python3 -m py_compile` — compiled successfully with no errors.
