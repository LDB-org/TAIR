Read app.py, then change the --workers default to 6. Preserve explicit overrides and all unrelated code. Use the available editing tools. After a successful edit, reply DONE. A configured project check runs automatically after each edit.