已完成。创建了 `classification-repeat-ten-20260920-final/02_textio.py`，提供 `write_text(path, text)` 和 `read_text(path)`，使用 pathlib 和 UTF-8，write_text 覆盖文件，read_text 返回完整字符串，不创建父目录，无顶层执行。已通过 `python3 -m py_compile` 编译检查，无错误。
