{
  "attempts": 40,
  "all_complete_and_passed": true,
  "requested_tests_review": "All four requested-test suites detect negative-total and input-mutation regressions.",
  "limitation": "One sample per family/configuration; arm names appear in separate working-directory paths. Subsequent targeted repeats and repository controls use a shared task path with isolated cache namespaces. Both native arms allow prefix caching. No codebook hits occurred; these timings do not measure warm reuse. Legacy CSV also passed, so a single pass does not establish a causal correctness repair."
}