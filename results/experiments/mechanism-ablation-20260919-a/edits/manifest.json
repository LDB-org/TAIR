{
  "cases": [
    [
      "cold",
      "Change --workers default to 6.",
      "import argparse\ndef build_parser():\n    p = argparse.ArgumentParser()\n    p.add_argument('--workers', type=int, default=4)\n    return p\n",
      6,
      false
    ],
    [
      "repeat",
      "Change --workers default to 6.",
      "import argparse\ndef build_parser():\n    p = argparse.ArgumentParser()\n    p.add_argument('--workers', type=int, default=4)\n    return p\n",
      6,
      false
    ],
    [
      "new_value",
      "Change --workers default to 8.",
      "import argparse\ndef build_parser():\n    p = argparse.ArgumentParser()\n    p.add_argument('--workers', type=int, default=4)\n    return p\n",
      8,
      false
    ],
    [
      "paraphrase",
      "Set the default value for --workers to 10.",
      "import argparse\ndef build_parser():\n    p = argparse.ArgumentParser()\n    p.add_argument('--workers', type=int, default=4)\n    return p\n",
      10,
      false
    ],
    [
      "source_change",
      "Change --workers default to 12.",
      "import argparse\ndef build_parser():\n    p = argparse.ArgumentParser()\n    p.add_argument('--workers', type=int, default=4)\n    return p\n# unrelated comment\n",
      12,
      false
    ],
    [
      "alias_cold",
      "Add alias -w to --workers.",
      "import argparse\ndef build_parser():\n    p = argparse.ArgumentParser()\n    p.add_argument('--workers', type=int, default=4)\n    return p\n",
      4,
      true
    ],
    [
      "alias_repeat",
      "Add alias -w to --workers.",
      "import argparse\ndef build_parser():\n    p = argparse.ArgumentParser()\n    p.add_argument('--workers', type=int, default=4)\n    return p\n",
      4,
      true
    ]
  ],
  "repeats": 3,
  "seed": 919,
  "native_tool": {
    "type": "function",
    "function": {
      "name": "edit",
      "description": "Replace one unique exact text occurrence in a file. Use the smallest sufficient replacement.",
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string"
          },
          "oldText": {
            "type": "string"
          },
          "newText": {
            "type": "string"
          }
        },
        "required": [
          "path",
          "oldText",
          "newText"
        ],
        "additionalProperties": false
      }
    }
  },
  "native": "ordinary auto tool call, no direct classifier or structured_outputs; same patched server ordinary path, not an unpatched vLLM binary",
  "method": "edit-only; same source/task/oracle; fresh state per repeat/arm; reset source each case; keep codebook across cases; interleaved arms; no retry; full client wall includes apply and validation; shared backend",
  "tokens": "native completion_tokens include serialized tool call; TAIR argument tokens and classification controls are separate"
}