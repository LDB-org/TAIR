Update config/settings.json: server.port=9090, retry.count=4, append tracing to features without removing metrics. Preserve all other fields, app.py, docs, and tests exactly. Run python -m unittest -v and summarize.