{
  "commit": "4772970c061837e78a8af23da946c0ecfb4c3d26",
  "cases": [
    [
      "cold",
      "Change --workers default to 6.",
      "import argparse\ndef build_parser():\n    p = argparse.ArgumentParser()\n    p.add_argument('--workers', type=int, default=4)\n    return p\n",
      6,
      false
    ],
    [
      "repeat",
      "Change --workers default to 6.",
      "import argparse\ndef build_parser():\n    p = argparse.ArgumentParser()\n    p.add_argument('--workers', type=int, default=4)\n    return p\n",
      6,
      false
    ],
    [
      "new_value",
      "Change --workers default to 8.",
      "import argparse\ndef build_parser():\n    p = argparse.ArgumentParser()\n    p.add_argument('--workers', type=int, default=4)\n    return p\n",
      8,
      false
    ],
    [
      "paraphrase",
      "Set the default value for --workers to 10.",
      "import argparse\ndef build_parser():\n    p = argparse.ArgumentParser()\n    p.add_argument('--workers', type=int, default=4)\n    return p\n",
      10,
      false
    ],
    [
      "source_change",
      "Change --workers default to 12.",
      "import argparse\ndef build_parser():\n    p = argparse.ArgumentParser()\n    p.add_argument('--workers', type=int, default=4)\n    return p\n# unrelated comment\n",
      12,
      false
    ],
    [
      "alias_cold",
      "Add alias -w to --workers.",
      "import argparse\ndef build_parser():\n    p = argparse.ArgumentParser()\n    p.add_argument('--workers', type=int, default=4)\n    return p\n",
      4,
      true
    ],
    [
      "alias_repeat",
      "Add alias -w to --workers.",
      "import argparse\ndef build_parser():\n    p = argparse.ArgumentParser()\n    p.add_argument('--workers', type=int, default=4)\n    return p\n",
      4,
      true
    ]
  ],
  "repeats": 2,
  "seed": 918,
  "arms": [
    "generate",
    "codebook"
  ],
  "scope": "edit only; existing default dynamic codebook; fresh state per repeat and arm; restore declared source before every task; retain codebook across tasks; no label cache; shared live backend; full bridge wall time including oracle; no retries"
}