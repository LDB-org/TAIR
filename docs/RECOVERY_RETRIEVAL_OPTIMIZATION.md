# 跳过强制恢复路径中不会使用的候选检索

2026-09-20。原流程在识别本轮必须走 recovery 分支前，已经检索内容/模板候选、计算
要求差异并构造候选 schema，随后又只保留 general recovery 选项。该工作不会进入模型请求。

现在先确定现有 recovery 状态，再在强制恢复时跳过候选检索。没有改变整轮失败/最新
结果两种恢复规则，没有减少可执行工具或增加推理请求；新用户任务或启用最新结果恢复
后成功的工具结果仍按原规则恢复检索。保留内容表条目计数用于日志；未读取模板表时
其条目数记录为 null，避免把未观察误记为零。新增 candidate_retrieval_skipped=recovery。

新增两项测试分别覆盖默认恢复和 latest 恢复，验证失败时不查询候选、恢复条件成立后
重新查询内容和模板。完整 508 项测试通过，归档 62623 个校验条目、2853 个导入文件通过。

## 本地量测

同一临时工作区，内容和模板各 2000 条合成记录，12 轮交错调用修改前/后的
generic_plan_chat；推理和能力查询使用测试桩，未请求 GPU 或执行工具。首轮作为冷启动
单独保留，以下中位数使用后 11 轮：

| 局部阶段 | 修改前 | 修改后 |
| --- | ---: | ---: |
| 候选准备日志阶段 | 5.08 ms | 0.12 ms |
| 整个本地 chat 调用（推理为桩） | 7.02 ms | 1.73 ms |

两个版本的模型消息、所有实际选项/schema、续写指令、分类提示、plan_budget 参数逐项
比较一致，保存比较值和 hash。对照旧源码取自 edit-state-ablation-20260920-a 的冻结快照；
并非比较不同模型生成轨迹。

这是每次强制恢复约 5 ms 的局部节省，不是端到端加速倍数。只涉及本地合成数据库，
表规模和内容不同可能改变节省量；没有解决模型误复用、错误测试或额外推理轮次。

[原始计时及输入一致性记录](RECOVERY_RETRIEVAL_PROFILE.json)
